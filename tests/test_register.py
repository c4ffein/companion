# TODO a test where a client tries to register on a non-running server
# TODO a test where a client tries to register on a server without the correct config
# TODO a test where a client tries to register on a server with the correct config but no admin rights
# TODO a test where a client tries to register on a server without the correct config and no admin rights
# TODO a test where a client tries to register on a server with the correct config and admin rights
#  - new user is created on the server
#  - new user is shown in the cli
#  - locks etc worked
# TODO same test but with a provided id
# TODO same test but with a provided id + password
